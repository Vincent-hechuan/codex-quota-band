# 正式候选包真机验收清单

> 注意：本文件主体保留的是 `0.3.x` AstroBox 日常桥接架构的历史验收证据，不能作为
> `0.5.2` 的当前安装步骤。当前 Android 新架构的组件链路、自动化结果和已完成真机证据
> 见 [build-verification.md](build-verification.md) 与 [architecture.md](architecture.md)。
> `0.5.2` 日常使用不需要 AstroBox 常驻；只有侧载或升级手环 RPK 时临时使用 AstroBox。
> 三端当前候选、已验收范围与未决冲突以 [current-status.md](current-status.md) 为准。

## 当前候选状态（2026-07-29）

当前状态：`0.5.0 / versionCode 500` 三端正式包已完成验收。修复手环“可用重置”数值继承周额度颜色的问题后，当前本地正式候选为
`0.5.2 / versionCode 502`：Windows、Android release APK 与手环 release RPK 均已正常升级并完成同步验收。手环已确认大于零的可用重置为绿色；零次为红色由自动化回归测试覆盖。
尚未提交、推送或发布。
AstroBox 只用于侧载或升级 RPK，不参与日常数据链路。

### Windows UI 与新手流程状态（2026-07-29）

- [x] 托盘图标、单实例托盘菜单、默认开机启动、刷新当前状态、二维码配对、连接与诊断、
  撤销手机配对、安装/修复任务 Hook 和退出入口已按当前候选行为核对。
- [x] 安装器完成页默认勾选「完成后启动 Codex额度并打开新手引导」；安装阶段先写入/修复任务 Hook，
  完成页启动已安装 EXE 并打开二维码引导。
- [x] 二维码窗口保留手机 App 扫码路径、同一可信局域网要求和 5 分钟有效期；配对教学窗支持拖动，
  有自绘关闭按钮和「我知道了」按钮，窗口不再默认贴屏幕左上角。
- [x] 连接与诊断显示 Windows 服务、Android 手机和 Codex 额度源；「立即确认」用于额度上游确认，
  不把局域网连接或 Hook 写入状态伪装成实时额度。
- [x] 实测二维码配对、教学窗拖动/关闭、诊断窗口定位和安装后托盘启动；Windows 原生 workspace
  最近一次 68 项测试全部通过。
- [x] 用户已确认 Windows 当前候选验收通过；这不等同于提交、推送或 GitHub Release 授权。

首次用户必须在 ChatGPT「设置 → 钩子 → 信任全部钩子」中确认
`PreToolUse`、`PermissionRequest`、`UserPromptSubmit`、`Stop` 已开启。
安装器写入 Hook 不能替代这一步。手机和电脑必须在同一可信局域网；遇到“任务状态不可用”时，按
局域网/VPN/防火墙 → 诊断 → 刷新状态 → 修复 Hook → ChatGPT 信任并重启的顺序排查。

### Android UI 当前页面与交互

- 首页当前为“额度与当前任务”头部、同步胶囊、离线提示（需要时）、周额度圆环、电脑/手环合并状态胶囊、当前任务摘要、可用重置和图标底部导航。正常额度使用红/黄/绿圆环；超过 60 秒未获上游确认时显示灰阶缓存状态。
- 任务页按需要授权、处理中、等待查看分组；左侧彩点与标题同一行，底部状态默认灰色，需要授权为红色。隐藏/删除必须二次确认，只隐藏本机和手环看板；新活动快照到达后允许恢复。
- 设置页分为连接与设备、提醒、显示与数据。扫码连接电脑使用系统相机；提醒开关保存到本机并分别控制手机/手环；系统通知设置跳转 Android 系统页面；显示与数据支持隐藏任务标题和导出诊断日志。
- 当前没有可独立点击的 Windows/手环设备卡片；手环检查由 Wearable bridge 刷新并请求权限，日常主连接仍由小米运动健康维护。

### 已确认真机结果、限制与待办

- 已有证据：真机 `c3f86dd8` 可安装并运行 `0.4.0 / versionCode 400`；Android 单元测试 83 项、lint 和 debug assemble 均通过；二维码配对、额度/任务同步、任务本机隐藏、通知和三页 UI 已完成阶段性验收。
- 已知限制：自动刷新只在 Android 进程存活时有保证；系统回收进程后不保证后台同步或通知必达。用户随后已确认当前 Windows、Android 与手环候选的联合验收通过；版本统一与发布操作仍未执行。
- 后续观察项：不同 OEM 深色模式/状态栏的细微对比度与模糊差异；通知时机辅助文案需随选项统一。设置页已提供“检查手环连接”入口，用于重新请求 Wearable 权限，不再是未决取舍。
- 产品语义保持不变：`Stop` 仍是“等待查看”，不表示终止或失败；缓存不会冒充实时数据，也不会猜测缺失额度或重置信息。

### 小米手环 10 UI 当前状态

- [x] 源码页面为 `band-app/src/pages/index/index.ux`，公共额度状态在
  `band-app/src/common/quota-state.cjs`，任务排序/限制在 `band-app/src/common/task-state.cjs`。
- [x] 目标画布固定为 `212×520`。根节点内是两页 Vela 原生纵向 `swiper`，上下滑动切页；不使用横向切页、
  自定义长滚动容器或手写分页动画。系统边缘侧滑返回没有被自定义手势拦截。
- [x] 首页顺序为：系统时间 → 同步胶囊 → 周额度与周重置日 → 分割线 → 可用重置次数 → 最近到期日。
  任务页保留系统时间、同步胶囊和最多三条任务，不再显示额外“任务”标题。
- [x] 任务状态已实现：处理中（蓝点）、需要授权（红点和红色状态文字）、等待查看（绿点）；每条有短标题、状态文字、
  紧凑相对时间，标题最多两行并省略，最多三条，授权优先。
- [x] 周额度和可用重置标题使用亮白；周额度数字为主要视觉，额度仍保留红/黄/绿语义；重置卡到期只显示月日。
  同步胶囊在新鲜 60 秒内显示“已同步”，超过后显示“缓存 + 相对分钟”。
- [x] `band-app` 使用 `npm run build` 构建；构建后通过 22/22 项手环页面、状态、版本和分页行为测试。
  相关测试主要在 `band-app/test/band10-layout.test.js`、`band-app/test/built-page.test.js`、
  `band-app/test/quota-state.test.js`、`band-app/test/task-state.test.js`。
- [x] 当前本地正式产物为 `band-app/dist/com.codex.quota.android.release.0.5.0.rpk`；用户已完成导入、重新授权与同步验收。具体大小和 SHA-256 见 `docs/build-verification.md` 的 0.5.0 章节。

### 已确认与仍待确认

- [x] 先前候选已在小米手环 10 真机上确认 212×520 胶囊屏可打开、顶部时间/同步胶囊可读、两页可上下滑动、
  周额度与可用重置的无卡片大字结构可读，且系统边缘侧滑返回可用。
- [x] 用户真机反馈已确认同步胶囊、百分号、`1 次`、到期日期和任务页信息层级需要持续做光学校正；这些调整已写入
  0.4.9 候选并完成自动化验证。
- [x] 用户已确认 0.4.9 的额度数字、重置区、任务彩点/时间避让、缓存转换以及原生 `swiper` 的手感和边缘侧滑返回通过验收。
- [x] 用户已确认当前 Windows、Android 与手环候选的三端验收通过；后续仅保留版本统一和发布操作，且不能自行执行。

### Android quota v3 上游确认检查（2026-07-29）

- [x] Android 与当前 Windows 候选协商 quota v3；WSS 在线且上游额度为 `current` 时显示“已同步”。
- [x] 首页、任务页和设置页使用同一受控短状态胶囊；真机动态文案完整、留白正常、未触碰圆角。
- [x] WSS 在线但上游失败时的“缓存/待确认”、电脑断开时的“离线”和 `99天+` 封顶由单元测试覆盖，
  不再把任务更新时间或局域网收包时间写成额度确认时间。
- [x] v1/v2 回退、v3 严格解析、未知字段、断线清空协商状态、无变化快照重连和手环摘要脱敏已覆盖。
- [x] 唯一已授权设备 `c3f86dd8` 已覆盖安装并检查首页、任务页和设置页；当前版本为 `0.5.0-rc.1 / versionCode 500`。
- [x] 真机下拉刷新使 Windows 上游 `lastAttemptAt` 从 01:14:15 更新至 01:15:15，成功后手机显示
  “已同步 刚刚”和“同步于 01:15”；不再只是重新连接 WSS。

### Android 自动刷新与手环缓存语义复验（2026-07-29）

- [x] Android 自动刷新在连接未完成协商或处于重连时不会丢失到期请求；前台连续 2 分钟能自行保持“已同步”，无需下拉刷新。
- [x] 后台锁屏 2 分钟，在未开启自启动与“不限制电池使用”时正确降为“缓存 1 分”，没有把旧额度冒充实时。
- [x] 开启自启动与“不限制电池使用”后，后台锁屏 2 分钟可保持同步；这提高进程存活率，但不改变 Android 不承诺被系统杀死后持续运行的边界。
- [x] 手环仍能快速接收摘要；Android 会将超过 60 秒的上游额度确认下发为“缓存”，不会仅因手机到手环传输成功而显示“已同步”。

### Android 同步时间与周额度层级精修（2026-07-29）

- [x] 同步胶囊的相对时间使用应用级整分钟时钟；整分钟延迟由单元测试覆盖，避免由 Compose 初次组合
  时刻冻结。真机跨分钟观察期间 Windows 也刚好发布了新快照，顶部状态相应从“已同步 9分”变为“已同步 刚刚”。
- [x] 首页同步信息只保留顶部胶囊；周额度卡移除“同步于 ……”与“额度已同步”等重复文案，
  仅保留圆环、重置日期和必要标题。
- [x] 额度百分比放大、字距收紧并进行光学居中，真机检查中数字居中、无截断，圆环仍是首页第一视觉层级。
- [x] 最新 Debug APK 已覆盖安装到唯一已授权设备 `c3f86dd8` 并启动；证据：
  `android-app/device-ui-clock-and-ring.png`、`android-app/device-ui-clock-tick.png`。

### Android 任务层级与重置日期精修（2026-07-29）

- [x] 周额度重置日期使用正文级常规字重，与下方任务标题层级接近，不再与“周额度”标题同重。
- [x] 任务页分组标题使用中性色；每条任务以左侧彩点表达状态，底部状态与时间默认使用辅助灰。
- [x] “需要授权”彩点和状态文字使用错误红；“等待查看”继续按已确认语义显示为绿色彩点、灰色文字，
  不把 Hook `Stop` 擅自解释为失败或终止。
- [x] 真机当前同时覆盖“处理中”和“等待查看”，布局、分隔、相对时间和操作入口均未溢出；
  授权红色映射由单元测试覆盖。
- [x] Debug APK 已覆盖安装到唯一已授权设备 `c3f86dd8`；证据：
  `android-app/device-ui-reset-weight.png`、`android-app/device-ui-task-dot-layout.png`。

### Android 自动额度刷新与圆环校正（2026-07-29）

- [x] 旧上游确认在应用启动后已正确显示“缓存 7分”，不再以“已同步 12分”等文案伪装为实时数据。
- [x] 首个 45 秒自动刷新周期未执行下拉操作，Windows 回送新快照后额度从 45% 更新为 43%，顶部显示
  “已同步 刚刚”；后续快照继续更新至 42%。
- [x] 圆环内外层同心、百分号小于数字并底部对齐；任务状态彩点与标题在同一行垂直居中。
- [x] Debug APK 已覆盖安装到唯一已授权设备 `c3f86dd8`；证据：
  `android-app/device-ui-auto-refresh-after.png`、`android-app/device-ui-task-dot-baseline.png`。

### Android 圆环中心数字精修（2026-07-29）

- [x] 圆环内已移除“剩余额度”，百分比成为唯一中心信息；数字组向右、向上微调，较小百分号底部对齐。
- [x] 最新 Debug APK 已覆盖安装到唯一已授权设备 `c3f86dd8`；证据：
  `android-app/device-ui-ring-centered-percent-raised.png`。

### Android 深色模式增量检查（2026-07-28）

- [x] 系统深色模式下首页标题、分组标题和卡片正文不再显示为黑色。
- [x] 已同步首页保留红/黄/绿额度圆环和提亮后的任务、连接状态色。
- [x] 离线/缓存首页使用灰阶额度圆环和顶部缓存状态，不把缓存数据显示为实时数据。
- [x] 任务页和设置页的卡片、辅助文字、分隔线、开关和底部导航在真机上清晰可读。
- [x] Debug APK 已覆盖安装到唯一已授权设备 `c3f86dd8`；当前版本为 `0.5.0-rc.1 / versionCode 500`。

### Windows 原生增量验收（2026-07-26，后续已由 2026-07-29 三端确认覆盖）

- [x] 托盘菜单可直接打开配对二维码；配对窗口只保留扫码所需说明，不再显示不可手动使用的临时码或局域网地址。
- [x] 配对说明明确引导用户在 ChatGPT「设置 → 钩子 → 信任全部钩子」完成信任，并在手机端「Codex额度」App 中扫码连接电脑。
- [x] 连接与诊断窗口只展示可验证的服务、手机和同步状态，不把“已写入”推断为 Hook 已信任。
- [x] 从托盘撤销手机配对后，Windows 主动关闭现有同步连接；手机可在无需退出后台或重启 App 的情况下进入离线状态。
- [x] 此前待验收项已由用户于 2026-07-29 的三端确认覆盖；仍不自动执行发布。

- [x] Windows → Android 首次二维码配对成功；二维码内的一次性验证码由 Android 自动读取，用户不需要手动输入。
- [x] Windows 配对窗口不再展示无法手动输入的验证码和局域网地址，只显示扫码说明与有效期。
- [x] Android 设置页保留可用的“扫码连接电脑”入口；手环日常蓝牙连接与授权由小米运动健康保持。
- [x] Android 首页、任务页和设置页按确认原型完成真机验收：额度圆环、合并后的电脑/手环状态胶囊、
  任务标题省略号、无文字底部导航和大圆角层级均可正常显示。
- [x] ChatGPT 状态卡已移除；顶部同步胶囊作为唯一同步状态表达，周额度卡不再重复显示“实时”。
- [x] 离线/缓存状态通过：灰阶背景、灰阶额度圆环和顶部缓存状态同时出现，不把缓存数据显示为实时数据。
- [x] 设置页“隐藏任务标题”和“导出本地诊断”入口可用；任务标题超出显示范围时追加“...”。
- [x] Android 设置页文字已改为面向首次用户的说明，不再使用“新版/旧版”等内部术语。
- [x] 安装最新 Windows 候选包后，确认完成页默认勾选会启动托盘程序并显示二维码引导；引导中的
  同一局域网和 ChatGPT「设置 → 钩子 → 信任全部钩子」审阅步骤清晰可用。
- [x] 重新安装最新 Windows 候选包后，确认安装器能在旧托盘进程运行时自动关闭并覆盖安装。
- [ ] 完成 Windows 与小米手环候选包的最终验收后，再由用户明确授权三端发布。

正式候选目标：Windows 原生 / Android APK / 小米手环 10 RPK 统一为同一产品版本。
前一手环 `0.4.9` 是历史 UI 覆盖候选；当前三端版本与发布条件见
[current-status.md](current-status.md)。

设备：Windows、Android 手机、小米手环 10

安全方案：0.4.0 使用 Windows 与 Android 的加密配对令牌；AstroBox Bearer 令牌仅属于旧版 0.3.x 插件，
只驻插件进程内存，不进入 0.4.0 日常链路。

## 0.3.x 历史验收记录

以下内容保留旧版 AstroBox 日常桥接架构的证据，不代表 0.4.0 当前安装方式或验收要求。

- [x] Windows 0.1.0 已静默安装到当前用户目录 `AppData\Local\Programs\CodexQuota`，安装器退出码 0。
- [x] Windows 卸载项、托盘进程和首次默认开机启动已验证。
- [x] Windows 服务监听 `0.0.0.0:17321`；无令牌读取返回 401。
- [x] 安装版持续写入闭合的 Snapshot v1；当前只包含 `weekly` 和服务端实际返回的 Full reset 摘要。
- [x] 正常托盘运行时，独立打包诊断仍可并行通过。
- [x] 开始菜单快捷方式、当前用户卸载项和 10 秒后台资源采样已验证；归一化 CPU 约 0.435%。
- [x] 系统托盘“显示配对信息…”已由用户实际点击并完成配对。
- [x] Android 手机已导入正式 ABP；检测到有效设备和订阅。
- [x] 首次配对成功；Windows 只保存一个 64 位十六进制令牌哈希，不含明文令牌。
- [x] 小米手环 10 的 RPK 0.1.1 已能打开、滚动并读取实时快照。
- [x] RPK 0.1.2 已完成真机测试；能同步，但整体过小、过密，灰色文字不可读。
- [x] 调整 Android 后台与 VPN 绕过设置后，AstroBox 已能保持长时间连接。
- [x] Android 真机确认 `source=plugdata&name=...&payload=...` 能把测试载荷直接交给指定 AstroBox 插件。
- [x] 安装 Windows 0.3.0 与 AstroBox 0.3.0，完成正式二维码自动配对验收。
- [x] 确认 Windows 0.3.0 通知区域显示新的高对比图标。
- [x] 安装 RPK 0.3.0，确认系统时间、紧凑同步胶囊、分割线及大字信息层级在真机正常。
- [ ] 对 0.3.0 完成 5 秒刷新、离线/重连和 10 分钟稳定性复验。

## 安装前

- 三台设备连接同一可信局域网。
- 如果 Android 手机开启 VPN，为 AstroBox 或电脑私网地址启用“绕过局域网”；该方式已确认能够保持连接。
- Android 手机上的 AstroBox 已获得蓝牙、附近设备和必要的后台运行权限。
- 侧载期间暂时强行停止“小米运动健康”，避免两个应用竞争手环蓝牙连接；完成验收后再测试恢复它时的实际影响。
- 保留旧往返 Spike，直到正式应用验收通过，便于失败时区分“传输环境”与“正式应用”问题。

## 1. Windows 候选包

安装 `dist/Codex Quota Setup 0.3.0.exe`。

验收：

- 安装范围为当前 Windows 用户，不要求管理员权限。
- 因个人侧载包未购买代码签名证书，Windows 显示“未知发布者”属于已知限制；应核对 SHA-256：`27D653E9AD8F6034C4CDC4DB21FEDB777D168C63DD7119149A7F157F43BCB43E`。
- 安装后没有主窗口，通知区域出现青绿色高对比「Codex 额度」托盘图标，不再是空白占位。
- 托盘摘要显示实际额度窗口和 Full reset 数量。
- “显示配对信息…”打开独立二维码窗口；手动地址与 6 位码默认折叠但可复制，窗口在配对码到期时自动关闭。
- “登录 Windows 时自动启动”首次默认为开；手动关闭后，退出并再次打开应用仍保持关闭。

## 2. Android / AstroBox 插件

在 AstroBox 中导入 `astrobox-plugin/dist/codex-quota-astrobox-0.3.0.abp`，SHA-256：`D5BCE87A7CB434E57C6D838486736094B4D2722CDB28F2576A904EFD487A3AD8`。安装后先手动打开一次插件，使其注册 DeepLink。

验收：

- 插件页面标题为「Codex 额度桥接 v0.3.0」。
- 页面明确显示“令牌只保存在本次 AstroBox 进程内”。
- 插件权限只有设备、对话框、设备通信、接收注册、DeepLink 注册和网络；没有短信、联系人、定位、相册等权限。
- 未配对时页面提示从 Windows 托盘打开二维码；“手动输入（高级）”仍可完成原有地址 + 6 位码流程。
- 点击“检测连接并重订阅”后，显示已连接设备数与订阅数。

## 3. 小米手环 10 RPK

在 AstroBox 中导入并安装 `band-app/dist/com.codex.quota.debug.0.3.0.rpk`，SHA-256：`7D7BEA5F1D160728ECE2AFDB55DBB65854A3212D40FF927923062BDAEBBE0EFF`。同包名升级失败时再卸载旧 RPK 后重装。

验收：

- 手环应用列表独立出现「Codex 额度」，包名为 `com.codex.quota`。
- 应用能打开并在单屏展示全部核心信息，不依赖滚动，不覆盖旧往返 Spike。
- 未配对时显示明确设置提示，不伪造 0% 或 100%。
- 页面使用小米手环 10 原生 212×520 画布，不再停留在启动图。
- 页面不再显示外缘进度环；保留 v0.1.3 的水平分割线与无卡片大字结构。
- 正常佩戴距离下，第一眼看到周额度百分比，其次是顶部系统时间和同步胶囊，最后是可用重置数量与到期日。
- 正常状态在紧凑胶囊中直接显示“已同步 10:08”，不使用中点；断线状态在同一固定区域显示“离线”和上次同步时间。
- “可用重置”与“周额度”字号一致；数量居中，数字有状态色，“次”为白色，到期副标题使用“7月27日到期”的短文案。

## 4. 首次配对

1. 先打开一次 AstroBox 0.3.0 插件，确认页面显示“已准备扫码配对”。
2. Windows 托盘选择“显示配对信息…”，电脑显示二维码。
3. 用 Android 系统相机扫码，选择在 AstroBox 中打开；应直接进入「Codex 额度桥接」并自动配对，不输入长地址或 6 位码。
4. 不在截图或聊天中发送配对二维码；其中的临时码 5 分钟过期、最多尝试 3 次且成功后立即失效。
5. 配对成功后点击一次“检测连接并重订阅”。扫码失败时才展开 Windows 手动信息，并点击插件“手动输入（高级）”。

升级 AstroBox 0.3.0 会启动新的插件进程，本候选需要重新扫码一次。后续只要 AstroBox 进程内令牌仍在，升级同包名 RPK 0.3.0 不需要再次配对；AstroBox 进程被系统终止后，再从 Windows 托盘生成新二维码即可。

验收：

- AstroBox 显示配对成功及有效订阅数。
- Windows 只保存长期令牌的 SHA-256 哈希；手机令牌只驻内存；手环不接收令牌。
- Windows 托盘“撤销所有已配对设备”能立即让旧令牌失效。

## 5. 正常链路

打开手环「Codex 额度」并计时。

验收：

- 5 秒内显示新快照，或在 5 秒边界显示明确超时；不得无限停留在“等待回包”。
- 只显示服务端实际返回的窗口。当前没有 5 小时窗口时，只显示周额度。
- 周额度显示剩余百分比、动态红/黄/绿颜色和重置日期；不再重复显示进度条或“剩余”。
- Full reset 只显示可用数量和最近到期日；逐条名称、精确到期时间和缓存来源不进入当前极简 MVP。
- 周额度与 Full reset 数量会随使用变化，不能把文档或截图中的数值当作固定期望值。
- 连续快速点击刷新不会产生并发请求。

## 6. 恢复与降级

逐项测试，每项保留一张不含配对码的截图和大致耗时：

1. 关闭并重开手环应用：先显示本地缓存，再刷新。
2. 临时关闭再打开蓝牙，AstroBox 进程保持存活：重新连接后不需要重新配对。
3. 退出 Windows 托盘：手环 5 秒内显示 Windows 不可达并保留缓存。
4. 重启 Windows 服务：配对哈希和最后可信快照仍存在，手机内存令牌仍在时自动恢复。
5. 强行停止并重启 AstroBox：预期显示未配对，从 Windows 托盘生成新二维码并扫码后恢复。
6. 暂停 Windows 同步：手环保留最后可信数据并明确显示暂停/旧数据状态。
7. 到了额度重置时间但 Codex 尚未写入新事件：显示待同步，不推断为 100%。
8. 重新启动“小米运动健康”，观察 10 分钟：记录 AstroBox 与手环是否被抢占断开。这一项决定当前架构能否作为日常 MVP 使用。

## 完成判定

只有上述正常链路、错误降级和 10 分钟连接稳定性都通过，才把正式 MVP 标记完成。若“小米运动健康”与 AstroBox 无法长期共存，候选包仍可证明技术闭环，但日常 MVP 的下一步应改为独立 Android 伴侣应用或其他不争抢主蓝牙连接的桥接方案。
