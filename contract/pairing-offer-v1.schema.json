{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://local.codex-quota.invalid/contract/pairing-offer-v1.schema.json",
  "title": "Codex Quota Encrypted Pairing Offer v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "protocolVersion",
    "type",
    "computerFingerprint",
    "endpoints",
    "code",
    "expiresAtMs"
  ],
  "properties": {
    "protocolVersion": { "const": 1 },
    "type": { "const": "pairing_offer" },
    "computerFingerprint": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "endpoints": {
      "type": "array",
      "minItems": 1,
      "maxItems": 8,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 128,
        "pattern": "^wss://[0-9.]+:[0-9]{1,5}/pair$"
      }
    },
    "code": {
      "type": "string",
      "pattern": "^[0-9]{6}$"
    },
    "expiresAtMs": { "type": "integer", "minimum": 0 }
  }
}
