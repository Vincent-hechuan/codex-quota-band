<script>
export default {
  onCreate() {
    console.info('Codex额度 v0.6.5 已启动')
  },
  onError(error) {
    console.error('Codex额度运行错误', error && error.message ? error.message : 'unknown')
  }
}
</script>
